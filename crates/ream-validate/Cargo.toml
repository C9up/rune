[package]
name = "ream-validate"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Validation engine in Rust for the Ream framework"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regex = "1"
